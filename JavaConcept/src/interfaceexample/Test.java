package interfaceexample;

public class Test {
	public static void main(String[] args) {
		// Java doesn't allow creating an object of an interface.
		/* WebDriver driver = new WebDriver(); */
		Child1 obj =new Child1();
		obj.show();
	}
}
